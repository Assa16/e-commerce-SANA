{{ form_start(form) }}
<div class="container mt-4">
<div class="jumbotron">
<div class="row">
<div class="col-md-2">{{ form_row(form.name) }}</div>
</div>
    {{ form_widget(form) }}
    <br>
    <button class="btn btn-primary btn-sm">{{ button_label|default('Sauvegarder') }}</button>
    </div>
    </div>
{{ form_end(form) }}
