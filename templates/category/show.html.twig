{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>{{ category.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Article</th>
                <td>
                        {% for cat in category.articles %}
                        <td>{{ cat.name }}</td>
                          
                        {% endfor %}
                </td>   
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}"><button type="button" class="btn btn-primary btn-sm">Retour à la liste de categorie</button></a>

    <a href="{{ path('category_edit', {'id': category.id}) }}"><button type="button" class="btn btn-primary btn-sm">Modifier la categorie</button></a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
