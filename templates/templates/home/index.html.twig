{% extends 'base.html.twig' %}

{% block title %}SANÃ{% endblock %}

{% block body %}
<!-- HEADER VIDEO -->
<div class="overlay"></div>
    <video playsinline="playsinline" autoplay="autoplay" muted="muted" loop="loop" style="width: 100%;;">
        <source src="{{ asset('img/header/header.mp4') }}" type="video/mp4">
    </video>
  <div class="container h-100">
    <div class="d-flex h-100 text-center align-items-center">
      <div class="w-100 text-white">
        <h1 class="display-3" style="font-family: 'Domine', serif;">SUMMER COLLECTION // 2019 PARIS</h1>
      </div>
    </div>
    <a href="{{ path('shop') }}"><button type="button" class="btn btn-primary">Shop Now</button></a>
  </div>
  
  
  <!-- /HEADER VIDEO -->
{% endblock %}
